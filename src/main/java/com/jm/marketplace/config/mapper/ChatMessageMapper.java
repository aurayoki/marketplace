package com.jm.marketplace.config.mapper;

public class ChatMessageMapper {
//    public static ChatMessage mapChatDtoToMessage(ChatMessageDto chatMessageDto) {
////        User fromUser = new User();
////        User toUser = new User();
//////        fromUser.setId(chatMessageDto.getSender());
//////        toUser.setId(chatMessageDto.getRecipient());
////
////        ChatMessage chatMessage = new ChatMessage();
////        chatMessage.setContent(chatMessageDto.getContent());
////        chatMessage.setSender(fromUser);
////        chatMessage.setRecipient(toUser);
////
////        return chatMessage;
//    }
}
